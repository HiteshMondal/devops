pipeline {
  agent any

  stages {
    stage('Build') {
      steps {
        sh 'cd app && npm install'
      }
    }

    stage('Docker Build') {
      steps {
        sh 'docker build -t devops-app:latest app'
      }
    }
  }
}
