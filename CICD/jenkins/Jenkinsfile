pipeline {
  agent any

  stages {
    stage('Build') {
      steps {
        sh 'docker build -t node-app ./app'
      }
    }

    stage('Test') {
      steps {
        sh 'echo "No tests yet"'
      }
    }

    stage('Deploy') {
      steps {
        sh 'echo "Deploy step here"'
      }
    }
  }
}
