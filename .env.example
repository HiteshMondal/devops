################################################################################
# DevOps Project — Environment Configuration
################################################################################
# Usage:
#   1. Copy this file to `.env`
#   2. Update ALL required values
#   3. Run: ./run.sh
#
# CI/CD Variables:
#   - GitHub Actions → Repo Settings → Secrets and variables → Actions
#   - GitLab         → Project Settings → CI/CD → Variables
################################################################################


################################################################################
# APPLICATION CONFIGURATION
################################################################################

APP_NAME=devops-app
NAMESPACE=devops-app
APP_PORT=3000

# Replica & Autoscaling configuration
REPLICAS=2
MIN_REPLICAS=2
MAX_REPLICAS=10
CPU_TARGET_UTILIZATION=70
MEMORY_TARGET_UTILIZATION=80

# Runtime configuration
APP_ENV=production
LOG_LEVEL=info

# Resource requests & limits
APP_CPU_REQUEST=100m
APP_CPU_LIMIT=500m
APP_MEMORY_REQUEST=128Mi
APP_MEMORY_LIMIT=512Mi


################################################################################
# INGRESS CONFIGURATION (OPTIONAL)
################################################################################

# Enable or disable Kubernetes Ingress
INGRESS_ENABLED=true

# Ingress hostname
# NOTE: For local clusters, add this to /etc/hosts
INGRESS_HOST=devops-app.local

# Ingress controller class
# Common values:
#   nginx   — minikube, kind, microk8s
#   traefik — k3s
#   alb     — AWS EKS
#   gce     — GKE
#   azure/application-gateway — AKS
INGRESS_CLASS=nginx

# TLS secret name (used mainly in production with cert-manager)
TLS_SECRET_NAME=devops-app-tls


################################################################################
# DOCKER / CONTAINER REGISTRY
################################################################################

# DockerHub credentials (required if BUILD_PUSH=true)
DOCKERHUB_USERNAME=yourdockerhubusername
DOCKERHUB_PASSWORD=your-dockerhub-password-or-token

# Image tag to use
DOCKER_IMAGE_TAG=latest


################################################################################
# GIT & GITHUB CONFIGURATION
################################################################################

# Git author configuration
GIT_AUTHOR_NAME=Your Name
GIT_AUTHOR_EMAIL=you@example.com

# GitHub configuration
GITHUB_USERNAME=yourgithubusername
GITHUB_EMAIL=yourmail@example.com
GITHUB_TOKEN=your-github-personal-access-token

# Repository URL
GIT_REPO_URL=https://github.com/User_Name/Project_Name.git


################################################################################
# DATABASE CONFIGURATION (OPTIONAL)
# Injected via Kubernetes ConfigMaps / Secrets
################################################################################

DB_HOST=localhost
DB_PORT=5432
DB_NAME=devopsdb
DB_USERNAME=
DB_PASSWORD=


################################################################################
# APPLICATION SECRETS (OPTIONAL)
# Stored as Kubernetes Secrets
################################################################################

JWT_SECRET=
API_KEY=
SESSION_SECRET=


################################################################################
# ARGO CD CONFIGURATION (OPTIONAL)
################################################################################

# Argo CD admin password
# If unset, it will be retrieved from the default admin secret
ARGOCD_ADMIN_PASSWORD=


################################################################################
# MLOPS CONFIGURATION
# All MLOps tools run automatically — no enable/disable flags needed.
# Just fill in API keys for the services you use; others are skipped gracefully.
################################################################################

# Pipeline runner: auto | metaflow | prefect | kubeflow | direct
MLOPS_PIPELINE_RUNNER=auto

# Data path (relative to project root)
MLOPS_DATA_PATH=data/raw

MLOPS_ACTION=full

################################################################################
# EVALUATION GATE
# Model must meet BOTH thresholds to replace the current serving model.
# Set both to 0.0 to always pass (useful during initial setup).
################################################################################

MLOPS_MIN_F1=0.0
MLOPS_MIN_ACCURACY=0.0

################################################################################
# NEPTUNE AI — Experiment Tracking (optional)
# Leave blank to skip — pipeline continues without it.
# Get token: https://app.neptune.ai/get_my_api_token
################################################################################

NEPTUNE_API_TOKEN=
NEPTUNE_PROJECT=

################################################################################
# COMET ML — Experiment Tracking (optional)
# Leave blank to skip.
# Get key: https://www.comet.com/api/my/settings
################################################################################

COMET_API_KEY=
COMET_PROJECT=devops-aiml
COMET_WORKSPACE=default

################################################################################
# MLFLOW — Self-hosted Experiment Tracking (optional)
# Defaults to in-cluster service. Override if running MLflow elsewhere.
################################################################################

MLFLOW_TRACKING_URI=http://mlflow-service.mlflow.svc.cluster.local:5000

################################################################################
# LAKEFS — Data Lake Versioning (optional)
# Leave blank to skip.
################################################################################

LAKEFS_ENDPOINT=http://localhost:8001
LAKEFS_ACCESS_KEY_ID=
LAKEFS_SECRET_ACCESS_KEY=

################################################################################
# WHYLOGS — Local Data Profiling (always runs, no API key needed)
# Profiles are saved to: monitoring/whylogs/profiles/
# Optional: upload to WhyLabs cloud dashboard
################################################################################

# WhyLabs upload (optional — whylogs local profiling works without this)
WHYLABS_API_KEY=
WHYLABS_ORG_ID=
WHYLABS_DATASET_ID=

################################################################################
# OPENLINEAGE — Data Lineage (optional)
# Leave blank to skip. Events are emitted to Marquez / DataHub if set.
################################################################################

OPENLINEAGE_URL=http://localhost:5000
OPENLINEAGE_NAMESPACE=devops-aiml

################################################################################
# DRIFT DETECTION
################################################################################

DRIFT_THRESHOLD=0.1