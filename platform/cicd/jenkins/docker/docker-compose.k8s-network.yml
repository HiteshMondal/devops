# docker-compose.k8s-network.yml — Local Kubernetes network attachment

services:
  jenkins:
    networks:
      - jenkins-net
      - k8s-net

networks:
  k8s-net:
    external: true
    name: ${K8S_DOCKER_NETWORK}
