ci:bootstrap:
  stage: validate
  image: alpine:latest
  script:
    - echo "🚀 CI pipeline initialized"

variables:
  DEPLOY_TARGET: "local"

stages:
  - validate
  - build
  - deploy-infra
  - deploy-app
  - deploy-services
  - verify

include:
  - local: cicd/gitlab/.gitlab-ci.yml