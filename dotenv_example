# ========================================
# DevOps Project Environment Configuration
# ========================================
# Update the values according to your setup
# For Github Action: Navigate to your repository → Settings → Secrets and variables → Actions 

# Project Configuration
PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
APP_NAME=devops-app
NAMESPACE=devops-app
APP_PORT=3000
REPLICAS=2
MIN_REPLICAS=2
MAX_REPLICAS=10
CPU_TARGET_UTILIZATION=70
MEMORY_TARGET_UTILIZATION=80

# Docker / Container Registry
# DockerHub credentials (required for pushing images)
DOCKERHUB_USERNAME=yourdockerhubusername
DOCKERHUB_PASSWORD=your-dockerhub-password-or-token
DOCKER_IMAGE_TAG=latest

# Kubernetes Configuration
KUBECONFIG_PROD=$HOME/.kube/config
KUBE_CONTEXT=minikube

# Image configuration
IMAGE_TAG=latest

# INGRESS CONFIGURATION
INGRESS_ENABLED=true
INGRESS_HOST=devops-app.local
INGRESS_CLASS=nginx
TLS_ENABLED=false
TLS_SECRET_NAME=devops-app-tls

# DATABASE CONFIGURATION (Secrets)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=devopsdb
DB_USERNAME=dbuser
DB_PASSWORD=SecurePassword123!

# APPLICATION SECRETS
JWT_SECRET=your-super-secret-jwt-key-change-this
API_KEY=your-api-key-here
SESSION_SECRET=your-session-secret-here

# MONITORING - PROMETHEUS
PROMETHEUS_ENABLED=true
PROMETHEUS_NAMESPACE=monitoring
PROMETHEUS_RETENTION=15d
PROMETHEUS_STORAGE_SIZE=10Gi
PROMETHEUS_SCRAPE_INTERVAL=15s
PROMETHEUS_SCRAPE_TIMEOUT=10s

# MONITORING - GRAFANA
GRAFANA_ENABLED=true
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin123
GRAFANA_PORT=3000
GRAFANA_STORAGE_SIZE=5Gi

# ALERTING CONFIGURATION
ALERT_EMAIL_ENABLED=false
ALERT_EMAIL_TO=alerts@example.com
ALERT_EMAIL_FROM=monitoring@example.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-email-password
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# RESOURCE LIMITS & REQUESTS
# Application
APP_CPU_REQUEST=100m
APP_CPU_LIMIT=500m
APP_MEMORY_REQUEST=128Mi
APP_MEMORY_LIMIT=512Mi
# Prometheus
PROMETHEUS_CPU_REQUEST=500m
PROMETHEUS_CPU_LIMIT=2000m
PROMETHEUS_MEMORY_REQUEST=1Gi
PROMETHEUS_MEMORY_LIMIT=4Gi
# Grafana
GRAFANA_CPU_REQUEST=100m
GRAFANA_CPU_LIMIT=500m
GRAFANA_MEMORY_REQUEST=256Mi
GRAFANA_MEMORY_LIMIT=1Gi

# AWS CONFIGURATION (for prod deployment)
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=123456789012
EKS_CLUSTER_NAME=devops-cluster
AWS_ACCESS_KEY_ID=ACCESSKEYEXAMPLE
AWS_SECRET_ACCESS_KEY=avErySampLEEXAMPLEKEY

# Git Configuration
GIT_AUTHOR_NAME=Your Name
GIT_AUTHOR_EMAIL=you@example.com

# GitHub configuration
GITHUB_USERNAME=yourgithubusername
GITHUB_EMAIL=yourmail@example.com
GITHUB_TOKEN=your-github-personal-access-token
GIT_REPO_URL=https://github.com/User_Name/Project_Name.git

# ArgoCD Sync Policy
ARGOCD_AUTO_SYNC=true
ARGOCD_SELF_HEAL=true
ARGOCD_PRUNE=true                           # Delete resources removed from Git
ARGOCD_SERVER_INSECURE=true                 # Disable TLS (set false for production)
ARGOCD_SYNC_RETRY_LIMIT=5                   # Number of sync retries on failure
ARGOCD_SYNC_TIMEOUT=300                     # Sync timeout in seconds
# (Optional)
ARGOCD_NOTIFICATIONS_ENABLED=false          # Enable Slack/email notifications
ARGOCD_SLACK_TOKEN=                         # Slack bot token
ARGOCD_SLACK_CHANNEL=                       # Slack channel for notifications

# Deployment Configuration
# Deployment target: 'local' for Minikube, 'prod' for AWS EKS
DEPLOY_TARGET=local

# Build and push images to DockerHub
BUILD_PUSH=false

# CI mode (affects certain behaviors)
CI=false

# Dry run mode (test without actual deployment)
DRY_RUN=false

# Minikube Configuration
# Enable Minikube ingress addon
MINIKUBE_INGRESS=true
MINIKUBE_MEMORY=4096
MINIKUBE_CPUS=2
